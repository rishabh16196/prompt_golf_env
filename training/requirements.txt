# Training deps — mirrors spaces_pipeline_env/colab/train_spaces_pipeline.ipynb
# torch is installed separately in hf_job_train.sh with the cu124 index URL,
# so it is intentionally NOT listed here.

transformers>=4.46.0
accelerate>=0.34.0
peft>=0.13.0
trl>=0.14.0
datasets>=3.0.0
bitsandbytes>=0.43.0; platform_system != "Darwin"
safetensors>=0.4.0
huggingface_hub>=0.26.0
sentencepiece>=0.2.0
matplotlib>=3.8.0
numpy>=1.24
