torch>=2.1
transformers>=4.45,<5.0
peft>=0.13
accelerate>=0.34
huggingface_hub>=0.26
# gradio 5.x uses the new Starlette API and handles hub 1.x cleanly.
# The 4.x pins kept hitting templates.TemplateResponse() positional-args
# breakage on starlette ≥ 0.40.
gradio>=5.0
sentencepiece
